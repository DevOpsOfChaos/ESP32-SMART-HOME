# NET-ERL Hall Light

Concrete device target for the hall light setup.

It is the production hall-light variant on the `net_erl` base type and combines relay control with the configured sensor set for that installation.
